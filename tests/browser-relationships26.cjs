const {chromium}=require('C:/Users/joshb/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({headless:true,channel:'msedge'}),p=await b.newPage({viewport:{width:390,height:844}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(process.env.TEST_URL||'http://127.0.0.1:4173');await p.locator('[data-tab="studio"]:visible').first().click();await p.locator('.industry-directory > summary').click();const card=p.locator('.company-head').first();await card.locator('details summary').click();await card.locator('[data-choice="decline"]').click();await p.reload();await p.locator('[data-tab="studio"]:visible').first().click();await p.locator('.industry-directory > summary').click();await card.locator('details summary').click();assert.match(await card.innerText(),/30\/100 trust/);assert.match(await card.innerText(),/eight weeks|8 weeks/);await card.locator('[data-choice="repair"]').click();assert.match(await card.innerText(),/40\/100 trust/);assert.match(await card.innerText(),/4 weeks/);assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth+1),false);assert.deepEqual(errors,[]);await p.screenshot({path:'test-results/relationships26.png'});await b.close();console.log('PASS relationship actions, reload persistence, repair and mobile layout');})().catch(e=>{console.error(e);process.exit(1)});
