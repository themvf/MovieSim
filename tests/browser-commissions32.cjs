const {chromium}=require('playwright');const assert=require('node:assert/strict');
(async()=>{const options={headless:true};if(process.env.CHROMIUM_EXECUTABLE)options.executablePath=process.env.CHROMIUM_EXECUTABLE;if(process.env.CHROMIUM_PACKAGE){const c=(await import(process.env.CHROMIUM_PACKAGE)).default;options.executablePath=await c.executablePath();options.args=c.args;}const b=await chromium.launch(options),p=await b.newPage({viewport:{width:390,height:844}}),errors=[];p.on('pageerror',e=>errors.push(e.message));const click=async q=>p.locator(q+':visible').first().click();await p.goto(process.env.TEST_URL||'http://127.0.0.1:4173');await p.evaluate(async()=>{const E=await import('./engine.js?v=0.32.0');localStorage.setItem('moviesim-save-v1',JSON.stringify(E.newGame(7)));localStorage.setItem('moviesim-build','0.10.0');});await p.reload();await click('[data-action="commission"]');assert((await p.locator('dialog').innerText()).includes('$600,000'));await p.screenshot({path:'test-results/commissions32-offer-mobile.png'});await click('[data-action="commissionAccept"]');assert((await p.locator('dialog').innerText()).includes('40 weeks remain'));await click('[data-action="commissionExtend"]');assert((await p.locator('dialog').innerText()).includes('$480,000'));await click('[data-action="scifiNew"]');await p.locator('[name="title"]').fill('The Colony');const labels=['Colony','Scientists','Artificial Intelligence','Survive','Isolation','Hopeful'];for(const [i,name]of labels.entries()){const card=i===5?'Thoughtful':name;await click(`[data-action="sfPick"][data-card="${card}"]`);}await click('[form="sf-form"]');await click('[data-action="commissionBind"]');assert((await p.locator('dialog').innerText()).includes('Required card is present'));await p.reload();
await p.evaluate(async()=>{const E=await import('./engine.js?v=0.32.0'),s=JSON.parse(localStorage.getItem('moviesim-save-v1')),m=s.movies[0];for(let i=0;i<3;i++){s.notices=[];E.act(s,'next');}const actors=s.people.filter(p=>p.kind==='actor').sort((a,b)=>a.fee-b.fee);for(let role=0;role<m.roles.length;role++){const a=actors[role];E.act(s,'audition',{id:m.id,person:a.id,role});E.act(s,'hire',{id:m.id,person:a.id,role,offer:E.quote(s,a,m,role).high});}const d=s.people.filter(p=>p.kind==='director').sort((a,b)=>a.fee-b.fee)[0];E.act(s,'audition',{id:m.id,person:d.id});E.act(s,'hire',{id:m.id,person:d.id,offer:E.quote(s,d,m).high});localStorage.setItem('moviesim-save-v1',JSON.stringify(s));});await p.reload();await click('[data-action="movie"]');await click('[data-action="production"]');await click('button[form="production-form"]');await click('[data-action="close"]');
for(let i=0;i<5;i++){await click('[data-action="next"]');if((await p.locator('dialog').count())&&await p.locator('dialog').isVisible()&&(await p.locator('dialog').innerText()).includes('A word from your director'))break;}
assert((await p.locator('dialog').innerText()).includes('A word from your director'));assert.equal(await p.locator('[data-action="event"]').count(),3);await p.screenshot({path:'test-results/commissions32-delay-mobile.png'});assert.equal(await p.locator('dialog').evaluate(el=>el.scrollWidth>el.clientWidth+1),false);await p.setViewportSize({width:1360,height:1000});await p.screenshot({path:'test-results/commissions32-delay-desktop.png'});await click('[data-action="event"][data-choice="wait"]');assert((await p.locator('dialog').innerText()).includes('Production paused'));await p.reload();let saved=await p.evaluate(()=>JSON.parse(localStorage.getItem('moviesim-save-v1')));assert(saved.movies[0].delayPlan.remaining>0);
await p.evaluate(async()=>{const E=await import('./engine.js?v=0.32.0'),s=JSON.parse(localStorage.getItem('moviesim-save-v1')),m=s.movies[0];for(let i=0;i<50&&m.stage!=='ready';i++){if(m.event)E.act(s,'event',{id:m.id,choice:m.event.kind==='delay'?'wait':'pay'});s.notices=[];E.act(s,'next');}E.act(s,'setRelease',{id:m.id,release:s.week+1});E.act(s,'confirmMarketing',{id:m.id,none:true});E.act(s,'distribute',{id:m.id,deal:'secure'});s.notices=[];E.act(s,'next');s.notices=[];m.openingRevealed=true;localStorage.setItem('moviesim-save-v1',JSON.stringify(s));});await p.reload();await click('[data-action="movie"]');assert((await p.locator('dialog').innerText()).includes('Mars Travel paid $480,000'));await click('[data-action="sequel"]');await click('[data-action="confirmSequel"]:not([data-rehire])');assert((await p.locator('dialog').innerText()).includes('Change sequel cards'));await click('[data-action="scifiEdit"]');await click('[data-card="Colony"]');await click('[data-card="Deep Space"]');await click('[form="sf-form"]');await p.reload();saved=await p.evaluate(()=>JSON.parse(localStorage.getItem('moviesim-save-v1')));assert.equal(saved.movies[1].stage,'development');assert.deepEqual(saved.movies[1].scifiCards.setting,['Deep Space']);assert.deepEqual(saved.movies[0].scifiCards.setting,['Colony']);assert.equal(saved.marsTravel.wins,1);assert.deepEqual(errors,[]);await b.close();console.log('PASS: commission offer/accept/extension, UI card creation/attachment, director delay popup, pause persistence, real release payment, immediate sequel card edits.');})().catch(e=>{console.error(e);process.exit(1)});
