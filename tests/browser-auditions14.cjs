const {chromium}=require('C:/Users/joshb/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({headless:true,channel:'msedge'});const page=await browser.newPage({viewport:{width:390,height:844}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(process.env.TEST_URL||'http://127.0.0.1:4173');await page.evaluate(async()=>{const E=await import('./engine.js');const s=E.newGame(57);s.cash=100000;const m=E.act(s,'buy',{script:s.market[0].id});for(const p of s.people){p.bookings=[];p.genres[m.genre]=85;p.star=20;p.fee=100;}localStorage.setItem('moviesim-save-v1',JSON.stringify(s));});await page.reload();
await page.locator('[data-action="movie"]').first().click();await page.locator('[data-action="director"]').first().click();assert.ok((await page.locator('.audition-limit').innerText()).includes('5 of 5'));assert.equal(await page.locator('[data-action="offer"]').count(),0);assert.ok(!(await page.locator('dialog').innerText()).includes('Expected direction'));await page.locator('[data-action="castingMode"]').click();
for(let i=0;i<5;i++)await page.locator('[data-action="audition"]').first().click();assert.equal(await page.locator('[data-action="audition"]').count(),0);assert.equal(await page.locator('[data-action="offer"]').count(),5);assert.ok((await page.locator('.audition-limit').innerText()).includes('0 of 5'));await page.locator('.audition-limit').scrollIntoViewIfNeeded();await page.screenshot({path:'test-results/auditions14-mobile.png'});
await page.reload();await page.locator('[data-action="movie"]').first().click();await page.locator('[data-action="director"]').first().click();assert.ok((await page.locator('.audition-limit').innerText()).includes('0 of 5'));
await page.evaluate(async()=>{const E=await import('./engine.js'),s=JSON.parse(localStorage.getItem('moviesim-save-v1'));s.notices=[];E.act(s,'next');localStorage.setItem('moviesim-save-v1',JSON.stringify(s));});await page.reload();await page.locator('[data-action="movie"]').first().click();await page.locator('[data-action="director"]').first().click();assert.ok((await page.locator('.audition-limit').innerText()).includes('5 of 5'));assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth+1),false);assert.deepEqual(errors,[]);console.log('PASS director forecast hidden, five auditions, negotiation unlock, save persistence, next-week reset and mobile layout');await browser.close();})().catch(e=>{console.error(e);process.exit(1)});
