const {chromium}=require('C:/Users/joshb/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({headless:true,channel:'msedge'});const p=await b.newPage({viewport:{width:390,height:844}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(process.env.TEST_URL||'http://127.0.0.1:4173');await p.locator('[data-tab="studio"]:visible').first().click();await p.locator('.industry-directory > summary').click();assert.equal(await p.locator('.company-head img').count(),10);await p.locator('.company-head img').evaluateAll(async imgs=>{for(const img of imgs){img.loading='eager';await img.decode();}});assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth+1),false);assert.deepEqual(errors,[]);await p.screenshot({path:'test-results/company-heads25.png'});await b.close();console.log('PASS ten company portraits and mobile directory');})().catch(e=>{console.error(e);process.exit(1)});
